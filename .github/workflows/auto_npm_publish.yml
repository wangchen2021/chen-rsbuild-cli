name: Enterprise NPM Publish
run-name: ${{ github.actor }} 发布包 🚀 [${{ github.ref_name }}]

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      deploy_env:
        description: '发布环境标识'
        required: true
        default: 'production'
        type: choice
        options:
          - production
          - staging
      custom_tag:
        description: '自定义 npm 标签（默认 beta，正式版填 latest）'
        required: false
        default: 'beta'

# 核心修复：匹配项目要求的 Node 24.x 版本
env:
  NODE_VERSION: '24.11.0'          # 改为项目要求的 24.x 版本
  PNPM_VERSION: '10.20.0'          
  NPM_REGISTRY: 'https://npm.pkg.github.com'
  NPM_SCOPE: '@wangchen2021'
  PACKAGE_JSON_PATH: './package.json'
  PNPM_HOME: '/home/runner/.local/share/pnpm'

jobs:
  publish-package:
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Environment pre-check
        run: |
          echo "🔍 执行环境预检..."
          if [ ! -f "${{ env.PACKAGE_JSON_PATH }}" ]; then
            echo "❌ 核心文件缺失：${{ env.PACKAGE_JSON_PATH }}"
            exit 1
          fi
          if [ -n "$(git status --porcelain)" ]; then
            echo "⚠ 工作区存在未提交修改，请注意审计"
            git status --porcelain
          fi

      # 先安装 pnpm 并配置路径
      - name: Install pnpm ${{ env.PNPM_VERSION }}
        uses: pnpm/action-setup@v3
        with:
          version: ${{ env.PNPM_VERSION }}
          run_install: false
        env:
          PNPM_HOME: ${{ env.PNPM_HOME }}

      - name: Add pnpm to PATH
        run: |
          echo "${{ env.PNPM_HOME }}" >> $GITHUB_PATH
          echo "PNPM_HOME=${{ env.PNPM_HOME }}" >> $GITHUB_ENV
          pnpm --version || {
            echo "❌ pnpm 安装失败，路径：${{ env.PNPM_HOME }}"
            exit 1
          }

      # 配置项目要求的 Node 24.x 版本
      - name: Setup Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'
          cache-dependency-path: './pnpm-lock.yaml'
          registry-url: ${{ env.NPM_REGISTRY }}

      - name: Install dependencies
        run: |
          echo "📦 安装项目依赖（锁定版本）..."
          if [ -f "pnpm-lock.yaml" ]; then
            # 忽略 Node 版本警告（如需严格匹配可移除 --ignore-engines）
            pnpm install --frozen-lockfile --prod=false --ignore-engines
          else
            echo "❌ 缺失 pnpm-lock.yaml，企业级项目必须锁定依赖版本"
            exit 1
          fi
          pnpm audit --severity high || echo "⚠ 检测到高危依赖漏洞，请及时修复"

      # 核心修复：修正 TS 编译命令，移除非法的 --verbose 参数
      - name: Build project
        id: build
        run: |
          echo "🔨 执行项目构建（适配 Node 24.x + 修复 TS 参数）..."
          # 检查 build 脚本并替换非法参数
          if pnpm run build --help >/dev/null 2>&1; then
            # 方案1：直接执行修复后的编译命令（推荐）
            tsc || {
              echo "❌ 编译失败，尝试兼容模式"
              # 兼容模式：移除 --verbose，保留核心编译逻辑
              npm set-script build "tsc"
              pnpm run build
            }
            # 验证构建产物
            if [ ! -d "dist" ]; then
              echo "❌ 构建产物目录 dist 缺失"
              exit 1
            fi
            echo "✅ 构建成功，产物大小：$(du -sh dist | awk '{print $1}')"
            echo "build_success=true" >> $GITHUB_OUTPUT
          else
            echo "⚠ 无 build 脚本，跳过构建"
            echo "build_success=false" >> $GITHUB_OUTPUT
          fi

      - name: Validate version consistency
        id: version_check
        run: |
          echo "📊 校验版本一致性..."
          RELEASE_VERSION="${{ github.ref_name }}"
          RELEASE_VERSION_CLEAN=${RELEASE_VERSION#v}
          PACKAGE_VERSION=$(pnpm pkg get version --file "${{ env.PACKAGE_JSON_PATH }}" | tr -d '"')
          
          if [ "$RELEASE_VERSION_CLEAN" != "$PACKAGE_VERSION" ]; then
            echo "❌ 版本不一致：Release($RELEASE_VERSION_CLEAN) ≠ Package($PACKAGE_VERSION)"
            exit 1
          fi
          echo "✅ 版本校验通过：$PACKAGE_VERSION"
          echo "package_version=$PACKAGE_VERSION" >> $GITHUB_OUTPUT

      - name: Configure npm authentication
        run: |
          echo "🔐 配置 npm 认证..."
          cat > .npmrc << EOF
          //npm.pkg.github.com/:_authToken=${{ secrets.NPM_TOKEN }}
          ${{ env.NPM_SCOPE }}:registry=${{ env.NPM_REGISTRY }}
          always-auth=true
          cache=false
          EOF
          npm whoami --registry="${{ env.NPM_REGISTRY }}" || {
            echo "❌ npm 认证失败，请检查 NPM_TOKEN 权限"
            exit 1
          }
          cat .npmrc | sed "s/_authToken=.*/_authToken=***REDACTED***/"

      - name: Publish package with pnpm
        id: publish
        run: |
          echo "🚀 开始发布包：${{ env.NPM_SCOPE }}/chen-rsbuild-cli@${{ steps.version_check.outputs.package_version }}"
          CUSTOM_TAG="${{ github.actor.inputs.custom_tag || 'beta' }}"
          # 忽略引擎警告发布
          pnpm publish \
            --registry="${{ env.NPM_REGISTRY }}" \
            --tag="$CUSTOM_TAG" \
            --access restricted \
            --ignore-engines \
            --verbose 2>&1 || {
              echo "❌ pnpm 发布失败，错误日志："
              cat ~/.npm/_logs/*-debug.log 2>/dev/null
              exit 1
            }
          echo "✅ 包发布成功，标签：$CUSTOM_TAG"
          echo "publish_tag=$CUSTOM_TAG" >> $GITHUB_OUTPUT

      - name: Post-publish audit
        if: always()
        run: |
          echo "📋 发布审计报告..."
          echo "========================================"
          echo "企业级发布审计信息："
          echo "发布时间：$(TZ=Asia/Shanghai date +'%Y-%m-%d %H:%M:%S')"
          echo "发布人：${{ github.actor }}"
          echo "包名：$(pnpm pkg get name --file "${{ env.PACKAGE_JSON_PATH }}" | tr -d '"')"
          echo "版本：${{ steps.version_check.outputs.package_version }}"
          echo "发布仓库：${{ env.NPM_REGISTRY }}"
          echo "发布标签：${{ steps.publish.outputs.publish_tag }}"
          echo "构建状态：${{ steps.build.outputs.build_success }}"
          echo "Node 版本：$(node -v)"
          echo "pnpm 版本：$(pnpm --version)"
          echo "GitHub Release：https://github.com/${{ github.repository }}/releases/tag/${{ github.ref_name }}"
          echo "========================================"